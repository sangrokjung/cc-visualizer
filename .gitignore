node_modules/
out/
dist/
.DS_Store

# tsc 자동 생성 (Vite가 .tsx 대신 resolve하는 문제 방지)
src/**/*.js
src/**/*.d.ts

# Claude Code (v6 Post-Dev Workflow)
.claude/handoff.md
.claude/checkpoints/
.claude/web-checklist-state.json
.claude/qa-*

# QA 산출물
*.png
.playwright-mcp/

# 개인 사용량 스냅샷 (ccusage 비용/토큰 — git 제외, dev 폴백용 로컬 생성)
# 재생성: npx ccusage daily --json | jq '{daily:[.daily[]|{period,totalTokens,totalCost,inputTokens,outputTokens,cacheCreationTokens,cacheReadTokens}],snapshotAt:(now|todate)}' > src/renderer/src/data/ccusage-snapshot.json
src/renderer/src/data/ccusage-snapshot.json

# Tauri Rust 빌드 캐시
src-tauri/target/

# macOS 메뉴바 데몬 Swift 빌드 산출물 (소스만 커밋, 바이너리는 build.sh로 재생성)
menubar/.build/

# 임시 worktree (병렬 에이전트 작업용)
.claude/worktrees/
.claude/scheduled_tasks.lock
.claude/sync-all-plan.json
.claude/sync-all-report.json

# 로컬 에이전트 하네스·실행 증거 (인증/세션 스냅샷 포함 가능)
.codex/
.omo/
.superpowers/
AGENTS.md
.claude/plan-*.md

# 외부 공유 전 사람 검토가 필요한 로컬 초안
docs/employee-email-draft.md
docs/qjc-webapp-download-page-pr.md
