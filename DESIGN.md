# cc-visualizer 메뉴바 UI 디자인 계약

## 0. 현재 화면 규칙
- 대상: macOS native 메뉴바 대시보드(TeamClaude·TeamCodex)
- 톤: 어두운 운영 대시보드, 상태 색상 중심
- 목적: 계정 상태를 숫자와 이름으로 한눈에 식별

## 1. 토큰
- 배경: `#0F131A` 계열
- 패널: `#18202A` 계열
- 본문: `#EBF2FA`
- 보조: `#8C9BB3`
- 사용 가능: 녹색
- 제한·확인 필요: 노란색
- 오류: 빨간색
- 비활성·구독 종료: 회색
- 본문 글꼴: system; 수치·계정명: monospaced system

## 2. 재사용 프리미티브
- 요약 카드: 상태 제목, 상태 pill, 4개 통계 타일
- 사용 가능 계정 strip: 판정된 사용 가능 계정 이름을 한 줄에 표시
- 서비스 비교 카드: TeamClaude·TeamCodex의 사용 가능 수와 계정명을 좌우 카드로 동시에 표시
- 계정 표: 계정명, 상태, 쿼터, 측정·복구·구독 액션
- TeamCodex 풀 카드: 사용 가능/풀/제외 요약과 계정 행

## 3. 접근성·상태
- 요약 숫자와 사용 가능 계정 이름은 동일한 상태 판정에서 생성한다.
- 계정 종료·제외·오류·제한 상태는 기존 색상 의미를 유지한다.
- 비교 카드는 사용 가능이면 녹색, 확인·제한이면 노란색, 연결 오류면 빨간색, 미연동이면 회색을 사용한다.
- 화면 폭 880pt 기준에서 이름 목록은 한 줄로 잘라 표시하고, 상세 상태는 행에서 확인한다.

## 4. 허용 부채
- 메뉴바 화면은 기존 Cocoa `draw(_:)` 기반이며, 이번 변경에서는 컴포넌트 계층을 교체하지 않는다.

## 비교 카드 가독성 (2026-09-14)
- 높이 240pt, 제목 24pt, 서비스명 20pt, 사용 가능 수 32pt, 계정명 17pt.
- 계정명은 별도 두 행으로 표시하고 폭을 넘으면 말줄임한다. 전체 이름은 접근성 요약과 상세 표에서 확인한다.

- 모델별 비교: TeamClaude Fable·Opus 카드를 첫 행에, TeamCodex를 둘째 행에 표시합니다. Opus는 Fable 전용 창만 제외하고 구독·일반 한도·최신 측정·동시성 조건은 동일하게 적용합니다. 요약 높이 396pt, 글꼴 크기는 유지합니다.

## 섹션 헤더·연속 페이지 (2026-09-23)
- 섹션 헤더 프리미티브(`DashboardSectionHeaderView`): 높이 28pt 띠. 제목은 `headFont`로 왼쪽 x16, 요약은 `smallFont`·muted로 오른쪽 끝 x−16에 우측 정렬, 바닥에 1px `line` 구분선, 배경은 `panel`. 색·글꼴은 `TeamClaudePalette`만 쓴다.
- 대시보드는 화면 높이에 맞춘 스크롤 문서 한 장이다. 섹션별 상한(계정 표 596pt, 전체 936pt)과 안쪽 스크롤은 없다. 요약 카드 아래로 섹션이 `헤더 28pt + 본문 + 간격 4pt`로 이어진다.
- 고정 헤더: 스크롤 뷰 위에 같은 프리미티브 하나가 떠서 지금 스크롤 위치의 섹션 이름·요약을 말한다. 요약 카드가 위에 보이는 동안은 숨긴다.
- 섹션 순서: Claude 풀 → Codex 풀 → Higgsfield → CLI 쿼터 → 사용량. 본문 높이가 0인 섹션은 헤더까지 통째로 생략한다.
